/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.youtube.com', 'i.vimeocdn.com', 'training.ols.to', 'localhost'], // Added localhost
  }
}

module.exports = nextConfig 